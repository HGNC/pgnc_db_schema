# pgnc-sql

Replace the pgnc-public.sql.gz file with the your own SQL file downloaded from the PGNC AWS DB instance.

This directory contains SQL database dumps from the PGNC public database.

## Notes

- Ensure the SQL dump is compressed in gzip format
- Keep the filename exactly as specified
- The file should contain a complete database dump with schema and data